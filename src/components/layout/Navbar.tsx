function Navbar() {
  return (
    <nav className="h-14 px-6 flex items-center justify-between bg-card border-b border-border">
      <span className="font-semibold">Student Portal</span>
    </nav>
  );
}

export default Navbar;
